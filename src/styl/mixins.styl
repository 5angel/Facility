size(width, height = width)
  width width
  height height

absolute(top = auto, right = auto, bottom = auto, left = auto)
  position absolute
  top top
  right right
  bottom bottom
  left left

absoluteCCW(top = auto, left = auto, bottom = auto, right = auto)
  absolute(top, right, bottom, left)
